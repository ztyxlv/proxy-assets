version = 1
[custom]
exclude_remarks = ["(Traffic|Expire|GB \\||G \\||Premium)"]

clash_rule_base = "base/mihomo.yaml"
surge_rule_base = "base/surge.conf"
singbox_rule_base = "base/sing-box.json"

[[custom_groups]]
name = "Proxy"
type = "select"
rule = [".*"]

[[rulesets]]
group = "Proxy"
ruleset = "[]MATCH"
